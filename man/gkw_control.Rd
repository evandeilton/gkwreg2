% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/control.R
\name{gkw_control}
\alias{gkw_control}
\title{Control Parameters for gkwreg2 Fitting}
\usage{
gkw_control(
  method = c("nlminb", "BFGS", "Nelder-Mead", "CG", "L-BFGS-B"),
  maxit = 1000L,
  reltol = 1e-08,
  trace = 0L,
  hessian = TRUE,
  start = NULL,
  keep_data = TRUE,
  use_gradient = TRUE
)
}
\arguments{
\item{method}{Optimization method. One of \code{"nlminb"} (default),
\code{"BFGS"}, \code{"Nelder-Mead"}, \code{"CG"}, or \code{"L-BFGS-B"}.}

\item{maxit}{Maximum number of iterations (default 1000).}

\item{reltol}{Relative convergence tolerance (default 1e-8).}

\item{trace}{Integer trace level: 0 = silent (default), 1+ = verbose.}

\item{hessian}{Logical; if \code{TRUE} (default), compute Hessian for
standard error estimation.}

\item{start}{Optional named list of starting values for coefficients.}

\item{keep_data}{Logical; if \code{TRUE} (default), keep data in fitted object.}

\item{use_gradient}{Logical; if \code{TRUE} (default), use analytical gradient.}
}
\value{
A list of control parameters with class \code{"gkw_control"}.
}
\description{
Specifies control parameters for the optimization algorithm used in
\code{\link{gkwreg2}}.
}
\examples{
# Default control
ctrl <- gkw_control()

# Use BFGS with more iterations
ctrl <- gkw_control(method = "BFGS", maxit = 2000)

# Disable Hessian computation for speed
ctrl <- gkw_control(hessian = FALSE)

}
